\chapter{Kiến trúc hệ thống}
\label{ch:phuong-phap-de-xuat}

\section{Sơ đồ Use Case}

Sơ đồ Use Case được sử dụng để xác định các tác nhân và nhóm chức năng nghiệp vụ chính của Student360 trước khi trình bày kiến trúc triển khai. Để bảo đảm khả năng đọc, báo cáo sử dụng một sơ đồ tổng quan và ba sơ đồ chi tiết theo miền nghiệp vụ. Sơ đồ tổng quan thể hiện phạm vi toàn hệ thống; các sơ đồ chi tiết lần lượt mô tả quản lý dữ liệu học thuật và bảng điểm, quản lý học bổng, và quản lý tài chính theo phương pháp 6 lọ. Trường đại học, khoa/chương trình đào tạo và môn học được xem là các đối tượng dữ liệu do quản trị viên quản lý, không phải tác nhân độc lập của hệ thống. Các bảng trong mục này đóng vai trò danh mục tóm tắt; đặc tả đầy đủ về điều kiện trước, luồng chính, luồng ngoại lệ và kết quả của từng Use Case được trình bày tại Chương~4.

\subsection{Sơ đồ Use Case tổng quan}

Hình~\ref{fig:usecase-overview} thể hiện ba tác nhân nghiệp vụ chính gồm sinh viên, nhà tài trợ và quản trị viên. Dịch vụ AI và Firebase Cloud Messaging là các hệ thống bên ngoài hỗ trợ gợi ý, tư vấn và gửi thông báo; chúng không thay thế quyết định nghiệp vụ của người dùng.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/usecase/usecase-overview.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/usecase/usecase-overview.png}%
    }{%
        \rectbox{Sơ đồ Use Case tổng quan hệ thống Student360}
    }
    \caption{Sơ đồ Use Case tổng quan hệ thống Student360}
    \label{fig:usecase-overview}
\end{figure}

\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{1.15}
    \caption{Các tác nhân trong hệ thống Student360}
    \label{tab:usecase-actors}
    \begin{tabular}{|C{2.8cm}|C{2.5cm}|L{8.2cm}|}
        \hline
        \textbf{Tác nhân} & \textbf{Phân loại} & \textbf{Vai trò trong hệ thống} \\
        \hline
        Sinh viên & Tác nhân chính & Sử dụng Mobile App để quản lý tài chính 6 lọ, bảng điểm cá nhân, hồ sơ học thuật, ứng tuyển học bổng, tương tác với AI và nhận thông báo. \\
        \hline
        Nhà tài trợ & Tác nhân chính & Sử dụng Webadmin để tạo và cấu hình chương trình học bổng, xem hồ sơ ứng tuyển và cập nhật trạng thái xét duyệt. \\
        \hline
        Quản trị viên & Tác nhân chính & Sử dụng Webadmin để quản lý danh mục trường đại học, khoa/chương trình đào tạo và môn học dùng chung trong hệ thống. \\
        \hline
        AI Service & Tác nhân hỗ trợ & Nhận yêu cầu thông qua Backend để phân loại giao dịch, hỗ trợ tư vấn tài chính, tạo nhận định và gợi ý học bổng phù hợp. AI không tự quyết định thay sinh viên hoặc nhà tài trợ. \\
        \hline
        Firebase Cloud Messaging & Hệ thống ngoài & Tiếp nhận yêu cầu gửi thông báo đẩy từ Backend, gồm thông báo thay đổi trạng thái học bổng và cảnh báo tài chính. \\
        \hline
    \end{tabular}
\end{table}

Ba tác nhân chính trực tiếp khởi tạo nghiệp vụ là sinh viên, nhà tài trợ và quản trị viên. AI Service và Firebase Cloud Messaging chỉ tham gia với vai trò hệ thống hỗ trợ; mọi dữ liệu nghiệp vụ đều được xác thực và điều phối qua Backend Student360.

\subsection{Sơ đồ Use Case quản lý dữ liệu học thuật và bảng điểm}

Trong Hình~\ref{fig:usecase-academic-gradebook}, quản trị viên chịu trách nhiệm chuẩn hóa danh mục trường, khoa/chương trình đào tạo và môn học. Sinh viên sử dụng các danh mục này để thiết lập thông tin học thuật, nhập hoặc import điểm, theo dõi GPA, tín chỉ tích lũy và mục tiêu tín chỉ. Quản trị viên không trực tiếp nhập hoặc chỉnh sửa bảng điểm cá nhân của sinh viên.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/usecase/usecase-academic-gradebook.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/usecase/usecase-academic-gradebook.png}%
    }{%
        \rectbox{Sơ đồ Use Case quản lý dữ liệu học thuật và bảng điểm}
    }
    \caption{Sơ đồ Use Case quản lý dữ liệu học thuật và bảng điểm}
    \label{fig:usecase-academic-gradebook}
\end{figure}

\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{1.15}
    \caption{Danh mục Use Case quản lý dữ liệu học thuật và bảng điểm}
    \label{tab:usecase-academic-gradebook}
    \begin{tabular}{|C{2.2cm}|L{4.2cm}|C{2.5cm}|L{4.8cm}|}
        \hline
        \textbf{Mã UC} & \textbf{Tên Use Case} & \textbf{Tác nhân chính} & \textbf{Mô tả tóm tắt} \\
        \hline
        \texttt{ACA-UC01} & Quản lý dữ liệu học thuật & Quản trị viên & Thêm, cập nhật, xóa, tìm kiếm và import danh mục trường, khoa/chương trình đào tạo và môn học trên Webadmin. \\
        \hline
        \texttt{ACA-UC02} & Quản lý bảng điểm cá nhân & Sinh viên & Chọn thông tin học thuật, thêm/sửa/xóa điểm, xem GPA, tín chỉ tích lũy và thiết lập mục tiêu tín chỉ. \\
        \hline
        \texttt{ACA-UC03} & Import bảng điểm từ tệp & Sinh viên & Đọc dữ liệu TXT/CSV trên Mobile App, ánh xạ môn học, cho phép kiểm tra và chỉnh sửa trước khi lưu điểm. \\
        \hline
    \end{tabular}
\end{table}

Quan hệ giữa hai tác nhân được thực hiện gián tiếp thông qua dữ liệu chuẩn hóa: quản trị viên duy trì danh mục học thuật, còn sinh viên chỉ sử dụng danh mục này trong hồ sơ và bảng điểm cá nhân.

\subsection{Sơ đồ Use Case quản lý và ứng tuyển học bổng}

Hình~\ref{fig:usecase-scholarship} phân tách rõ trách nhiệm giữa sinh viên và nhà tài trợ. Sinh viên tìm kiếm, chuẩn bị, nộp và theo dõi hồ sơ; nhà tài trợ công bố chương trình, cấu hình điều kiện và xét duyệt hồ sơ. Chức năng AI gợi ý học bổng là phần hỗ trợ mở rộng, không thuộc luồng nghiệp vụ bắt buộc của quá trình tạo, nộp và xét duyệt học bổng.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/usecase/usecase-scholarship.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/usecase/usecase-scholarship.png}%
    }{%
        \rectbox{Sơ đồ Use Case quản lý và ứng tuyển học bổng}
    }
    \caption{Sơ đồ Use Case quản lý và ứng tuyển học bổng}
    \label{fig:usecase-scholarship}
\end{figure}

\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{1.15}
    \caption{Danh mục Use Case quản lý và ứng tuyển học bổng}
    \label{tab:usecase-scholarship}
    \begin{tabular}{|C{2.2cm}|L{4.0cm}|C{2.5cm}|L{5.0cm}|}
        \hline
        \textbf{Mã UC} & \textbf{Tên Use Case} & \textbf{Tác nhân chính} & \textbf{Mô tả và tác nhân hỗ trợ} \\
        \hline
        \texttt{SCH-UC01} & Quản lý chương trình học bổng & Nhà tài trợ & Tạo, cập nhật và quản lý học bổng; cấu hình điều kiện ứng tuyển và danh sách tài liệu yêu cầu. \\
        \hline
        \texttt{SCH-UC02} & Ứng tuyển học bổng trực tuyến & Sinh viên & Tìm kiếm học bổng, chuẩn bị tài liệu, lưu nháp, nộp, theo dõi, bổ sung hoặc hủy hồ sơ. AI Service có thể hỗ trợ gợi ý học bổng nhưng không tham gia quyết định nộp hồ sơ. \\
        \hline
        \texttt{SCH-UC03} & Xét duyệt hồ sơ học bổng & Nhà tài trợ & Xem và đánh giá hồ sơ, cập nhật trạng thái xét duyệt; Backend sử dụng Firebase Cloud Messaging để thông báo kết quả tới sinh viên. \\
        \hline
    \end{tabular}
\end{table}

Luồng nghiệp vụ cốt lõi từ tạo học bổng tới xét duyệt không phụ thuộc AI. Chức năng gợi ý chỉ mở rộng bước sinh viên tìm kiếm học bổng, còn kết quả xét duyệt do nhà tài trợ cập nhật.

\subsection{Sơ đồ Use Case quản lý tài chính theo phương pháp 6 lọ}

Hình~\ref{fig:usecase-six-jars-finance} mô tả các chức năng tài chính do sinh viên trực tiếp sử dụng, gồm quản lý sáu lọ mặc định (Thiết yếu, Giáo dục, Đầu tư, Thiện tâm, Hưởng thụ và Tiết kiệm), thiết lập tỷ lệ phân bổ, ghi nhận giao dịch, chuyển tiền, lập lịch và theo dõi báo cáo. AI hỗ trợ đề xuất lọ phù hợp cho giao dịch, tạo nhận định tài chính và trả lời câu hỏi; sinh viên vẫn là người xác nhận hoặc điều chỉnh kết quả trước khi lưu dữ liệu nghiệp vụ.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/usecase/usecase-six-jars-finance.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/usecase/usecase-six-jars-finance.png}%
    }{%
        \rectbox{Sơ đồ Use Case quản lý tài chính theo phương pháp 6 lọ}
    }
    \caption{Sơ đồ Use Case quản lý tài chính theo phương pháp 6 lọ và AI hỗ trợ}
    \label{fig:usecase-six-jars-finance}
\end{figure}

\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{1.15}
    \caption{Danh mục Use Case quản lý tài chính 6 lọ và AI hỗ trợ}
    \label{tab:usecase-six-jars-finance}
    \begin{tabular}{|C{2.2cm}|L{4.0cm}|C{2.3cm}|L{5.2cm}|}
        \hline
        \textbf{Mã UC} & \textbf{Tên Use Case} & \textbf{Tác nhân chính} & \textbf{Mô tả và tác nhân hỗ trợ} \\
        \hline
        \texttt{FIN-UC01} & Cấu hình tỷ lệ 6 lọ & Sinh viên & Xem các lọ tài chính, điều chỉnh tỷ lệ phân bổ và quản lý cấu hình lọ cá nhân. \\
        \hline
        \texttt{FIN-UC02} & Quản lý giao dịch thủ công & Sinh viên & Ghi nhận, cập nhật, xóa và xem lịch sử giao dịch thu/chi gắn với từng lọ tài chính. \\
        \hline
        \texttt{FIN-UC03} & Phân bổ thu nhập tự động & Sinh viên & Chia một khoản thu vào các lọ theo tỷ lệ mặc định hoặc theo cấu hình phân bổ do sinh viên lựa chọn. \\
        \hline
        \texttt{FIN-UC04} & Cảnh báo tài chính theo lọ & Sinh viên & Cấu hình ngưỡng ngân sách hoặc số dư; Firebase Cloud Messaging hỗ trợ chuyển cảnh báo tới thiết bị. \\
        \hline
        \texttt{FIN-UC05} & Lập lịch giao dịch định kỳ & Sinh viên & Tạo, cập nhật, kích hoạt, tạm dừng hoặc xóa lịch giao dịch tự động theo chu kỳ. \\
        \hline
        \texttt{AI-UC01} & Trợ lý AI tài chính & Sinh viên & Đặt câu hỏi và nhận phản hồi tài chính theo thời gian thực; AI Service xử lý ngôn ngữ tự nhiên và tạo nội dung tư vấn. \\
        \hline
        \texttt{AI-UC02} & AI phân loại giao dịch & Sinh viên & AI Service đề xuất lọ và danh mục từ mô tả giao dịch; sinh viên xác nhận hoặc điều chỉnh trước khi lưu. \\
        \hline
    \end{tabular}
\end{table}

Trong phân hệ này, AI chỉ hỗ trợ phân loại, nhận định và hỏi đáp. Các thao tác làm thay đổi số dư, tỷ lệ phân bổ hoặc lịch giao dịch chỉ được thực hiện sau yêu cầu hoặc xác nhận của sinh viên.

\section{Tổng quan kiến trúc hệ thống}

\subsection{Mục tiêu kiến trúc}
Kiến trúc của hệ thống Student360 được thiết kế nhằm đạt được bốn mục tiêu chính: (1) phân tách rõ ràng trách nhiệm giữa các thành phần (Separation of Concerns), giúp mỗi phần chỉ tập trung xử lý đúng vai trò của mình; (2) cho phép các thành phần được phát triển, kiểm thử và vận hành độc lập với nhau; (3) hỗ trợ nhiều thành viên trong nhóm phát triển song song trên các phần khác nhau của hệ thống mà không xung đột mã nguồn với nhau, phù hợp với cách phân công theo cặp đã trình bày ở Chương 5; (4) cô lập được rủi ro và chi phí vận hành của thành phần Trí tuệ nhân tạo (độ trễ, chi phí gọi mô hình ngôn ngữ lớn) khỏi luồng nghiệp vụ lõi của hệ thống.

\subsection{Mô hình kiến trúc tổng thể}
Hệ thống được tổ chức theo mô hình \textbf{Kiến trúc Client--Server phân tán nhiều tầng}, gồm ba tầng logic: tầng trình bày (Presentation Tier), tầng ứng dụng/xử lý nghiệp vụ (Application Tier) và tầng dữ liệu (Data Tier). Về mặt triển khai mã nguồn, mỗi thành phần thuộc tầng trình bày và tầng ứng dụng được hiện thực trong một repository Git độc lập (đa-repository), không dùng chung một repository duy nhất. Các thành phần giao tiếp với nhau hoàn toàn qua mạng, thông qua các giao thức kết nối tiêu chuẩn như HTTP REST, Server-Sent Events (SSE) và hàng đợi thông điệp (Message Queue), chứ không chia sẻ trực tiếp mã nguồn hay tiến trình với nhau. Cách tổ chức này cho phép mỗi dịch vụ được phát triển, triển khai và mở rộng độc lập, đồng thời phù hợp với quy mô một nhóm nhỏ (6 thành viên) khi mỗi cặp thành viên có thể làm việc trọn vẹn trên repository của mình mà ít va chạm với phần việc của cặp khác.

\subsection{Các thành phần chính và vai trò}
\begin{itemize}
    \item \textbf{Mobile App:} Kênh tương tác chính của sinh viên với hệ thống, thuộc tầng trình bày.
    \item \textbf{Web Admin Portal:} Công cụ quản trị dành cho nhà tài trợ và quản trị viên, thuộc tầng trình bày.
    \item \textbf{Backend:} Trung tâm xử lý nghiệp vụ, xác thực, phân quyền và điều phối truy cập dữ liệu, thuộc tầng ứng dụng.
    \item \textbf{AI Service:} Dịch vụ chuyên trách xử lý ngôn ngữ tự nhiên và các tác vụ liên quan tới Trí tuệ nhân tạo, thuộc tầng ứng dụng.
    \item \textbf{Tầng lưu trữ (PostgreSQL và Redis):} Nơi lưu trữ dữ liệu nghiệp vụ và quản lý hàng đợi xử lý bất đồng bộ, thuộc tầng dữ liệu.
\end{itemize}

Hình~\ref{fig:context-diagram} mô tả sơ đồ ngữ cảnh hệ thống (System Context Diagram), thể hiện các tác nhân tương tác với hệ thống và các hệ thống bên ngoài mà Student360 phụ thuộc vào.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1.1cm and 2.6cm,
        actor/.style={draw, rounded corners, minimum width=3cm, minimum height=1.05cm, align=center, fill=blue!8, font=\small},
        system/.style={draw, thick, rounded corners, minimum width=4cm, minimum height=4.6cm, align=center, fill=blue!15, font=\small},
        ext/.style={draw, rounded corners, minimum width=3.3cm, minimum height=1.05cm, align=center, fill=gray!12, font=\small},
        lbl/.style={font=\tiny, align=center}
    ]
        \node[system] (sys) {\shortstack{\textbf{Hệ thống Student360}\\[3pt]\footnotesize Quản lý tài chính,\\học bổng và học thuật\\cho sinh viên}};

        \node[actor, left=of sys, yshift=1.7cm] (sv) {Sinh viên};
        \node[actor, left=of sys] (nt) {Nhà tài trợ};
        \node[actor, left=of sys, yshift=-1.7cm] (qt) {Quản trị viên};

        \node[ext, right=of sys, yshift=1.4cm] (llm) {\shortstack{Nhà cung cấp\\Mô hình ngôn ngữ lớn}};
        \node[ext, right=of sys, yshift=-1.4cm] (fcm) {\shortstack{Firebase Cloud\\Messaging}};

        \draw[->] (sv) -- node[lbl, above] {Ghi nhận chi tiêu, trò chuyện AI} (sys.west |- sv);
        \draw[->] (nt) -- node[lbl, above] {Đăng và duyệt học bổng} (sys.west |- nt);
        \draw[->] (qt) -- node[lbl, below] {Quản trị dữ liệu học thuật} (sys.west |- qt);

        \draw[->] (sys.east |- llm) -- node[lbl, above] {Yêu cầu suy luận ngôn ngữ tự nhiên} (llm);
        \draw[->] (sys.east |- fcm) -- node[lbl, below] {Gửi thông báo đẩy} (fcm);
    \end{tikzpicture}
    \caption{Sơ đồ ngữ cảnh hệ thống (System Context Diagram) của Student360}
    \label{fig:context-diagram}
\end{figure}

Ở mức chi tiết hơn, Hình~\ref{fig:container-diagram} trình bày sơ đồ kiến trúc Container, thể hiện các thành phần triển khai cụ thể (Mobile App, Web Admin Portal, Backend, AI Service, hàng đợi và tầng lưu trữ) cùng giao thức giao tiếp giữa chúng.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/architecture/system-container.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/architecture/system-container.png}%
    }{%
        \rectbox{Sơ đồ kiến trúc Container của hệ thống Student360}
    }
    \caption{Sơ đồ kiến trúc Container của hệ thống Student360}
    \label{fig:container-diagram}
\end{figure}

\subsection{Luồng hoạt động tổng quát}
Người dùng thao tác trên Mobile App hoặc Web Admin Portal, gửi yêu cầu tới Backend qua giao thức HTTP REST thông thường, hoặc qua kênh SSE riêng đối với chức năng trò chuyện AI cần phản hồi theo thời gian thực. Backend xác thực danh tính và phân quyền truy cập, sau đó xử lý nghiệp vụ và đọc/ghi dữ liệu trực tiếp trên PostgreSQL. Đối với các yêu cầu cần năng lực xử lý ngôn ngữ tự nhiên (tư vấn tài chính, phân loại giao dịch, gợi ý học bổng), Backend đóng vai trò cổng trung gian, chuyển tiếp yêu cầu sang AI Service; AI Service không truy cập trực tiếp vào cơ sở dữ liệu mà luôn gọi ngược lại Backend để lấy dữ liệu thật khi cần suy luận. Kết quả xử lý cuối cùng được trả ngược qua Backend về lại giao diện người dùng. Đối với các tác vụ không cần phản hồi tức thời (gửi thông báo hàng loạt, xử lý nền định kỳ), Backend đẩy tác vụ vào hàng đợi Redis để xử lý bất đồng bộ, tránh làm chậm các yêu cầu API chính.

\section{Kiến trúc Frontend}

\subsection{Ứng dụng Mobile}
Ứng dụng Mobile (Expo/React Native) là kênh tương tác chính của sinh viên với hệ thống, đảm nhận việc ghi nhận thu chi tài chính, trò chuyện với trợ lý AI, ứng tuyển học bổng, quản lý bảng điểm cá nhân và nhận thông báo đẩy.

Về mặt tổ chức, ứng dụng được chia thành ba tầng logic tách biệt: tầng giao diện và định tuyến, tổ chức theo mô hình định tuyến dựa trên tệp tin (file-based routing) giúp cấu trúc màn hình phản ánh trực tiếp cấu trúc điều hướng của ứng dụng; tầng trạng thái, tách biệt rõ giữa trạng thái phiên đăng nhập/giao diện cục bộ và trạng thái dữ liệu nghiệp vụ được đồng bộ từ máy chủ; và tầng giao tiếp API, tập trung toàn bộ việc gọi Backend qua một lớp client duy nhất.

Về xử lý dữ liệu, trạng thái phiên đăng nhập và các trạng thái giao diện cục bộ (như phiên trò chuyện AI, bộ lọc hiển thị) được quản lý độc lập với trạng thái dữ liệu nghiệp vụ lấy từ Backend. Dữ liệu nghiệp vụ được đồng bộ hai chiều với Backend thông qua cơ chế làm mới bộ đệm tự động mỗi khi có thay đổi (đã trình bày cơ chế cụ thể ở Chương 6, mục Cơ chế đồng bộ trạng thái giao diện tức thời), giúp số dư và lịch sử giao dịch trên màn hình luôn phản ánh đúng dữ liệu mới nhất mà không cần người dùng tải lại thủ công. Lớp giao tiếp API tự động đính kèm token xác thực vào mỗi yêu cầu và tự động làm mới token khi hết hạn, giúp phiên đăng nhập của người dùng được duy trì liền mạch.

Việc lựa chọn nền tảng Expo/React Native cho phép phát triển đồng thời cho cả iOS và Android từ một mã nguồn duy nhất, phù hợp với quy mô và thời gian phát triển hạn chế của một đồ án tốt nghiệp. Thư viện thiết kế giao diện Tamagui được sử dụng nhất quán trong toàn bộ ứng dụng, đảm bảo giao diện đồng bộ về phong cách và tối ưu về hiệu năng hiển thị trên thiết bị di động.

Hình~\ref{fig:mobile-architecture} mô tả cách ứng dụng Mobile phân tách lớp định tuyến và giao diện, trạng thái cục bộ, dữ liệu máy chủ và lớp giao tiếp API. Expo Router đảm nhận điều hướng dựa trên cấu trúc tệp; Zustand quản lý trạng thái cục bộ; React Query quản lý dữ liệu lấy từ Backend và cơ chế làm mới cache; Axios client tập trung xử lý token và các yêu cầu HTTP.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/frontend/mobile-architecture.png}{%
        \includegraphics[width=\textwidth,height=0.75\textheight,keepaspectratio]{Images/chapter3/frontend/mobile-architecture.png}%
    }{%
        \rectbox{Sơ đồ kiến trúc ứng dụng Mobile}
    }
    \caption{Kiến trúc ứng dụng Mobile Student360}
    \label{fig:mobile-architecture}
\end{figure}

\subsection{Web Admin}
Web Admin Portal (React JS/Vite) là công cụ dành cho quản trị viên và nhà tài trợ, phục vụ việc quản lý dữ liệu học thuật chuẩn hóa (trường, chương trình đào tạo, môn học) và toàn bộ vòng đời của một chương trình học bổng, từ khởi tạo, cấu hình điều kiện cho tới xét duyệt hồ sơ ứng tuyển của sinh viên.

Về mặt tổ chức, Web Admin Portal được chia theo miền nghiệp vụ, mỗi miền là một khối chức năng độc lập có luồng dữ liệu và giao diện riêng. Việc kiểm soát truy cập theo vai trò (quản trị viên hay nhà tài trợ) được thực hiện ngay ở tầng định tuyến, đảm bảo mỗi vai trò chỉ nhìn thấy và thao tác được trên đúng phần giao diện được phép truy cập.

Trạng thái đăng nhập và thông tin phân quyền của người dùng được quản lý tập trung trong toàn ứng dụng, trong khi dữ liệu nghiệp vụ của từng miền chức năng (danh sách trường, danh sách học bổng, hồ sơ ứng tuyển...) được đồng bộ riêng theo từng khu vực giao diện tương ứng. Giống như ứng dụng Mobile, Web Admin Portal giao tiếp với Backend qua một lớp client tập trung, tự động đính kèm token xác thực vào mỗi yêu cầu.

Việc lựa chọn React JS kết hợp Vite giúp tối ưu tốc độ phát triển và build ứng dụng, trong khi thư viện Material UI cho phép dựng nhanh các giao diện quản trị dữ liệu dạng bảng với khối lượng bản ghi lớn, vốn là nhu cầu chính của một cổng quản trị.

Hình~\ref{fig:webadmin-architecture} thể hiện cấu trúc Web Admin Portal. React Router và lớp kiểm tra vai trò kiểm soát truy cập; các trang được tổ chức theo miền quản trị hoặc nhà tài trợ; React Query và Zustand quản lý dữ liệu máy chủ và trạng thái cục bộ; lớp service sử dụng Axios client để giao tiếp tập trung với Backend.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/frontend/webadmin-architecture.png}{%
        \includegraphics[width=\textwidth,height=0.75\textheight,keepaspectratio]{Images/chapter3/frontend/webadmin-architecture.png}%
    }{%
        \rectbox{Sơ đồ kiến trúc Web Admin Portal}
    }
    \caption{Kiến trúc Web Admin Portal của Student360}
    \label{fig:webadmin-architecture}
\end{figure}

\section{Kiến trúc Backend}

\subsection{Vai trò}
Backend, hiện thực trên nền tảng NestJS \cite{nestjs-docs}, là trung tâm điều phối của toàn hệ thống: xác thực người dùng, phân quyền truy cập, xử lý toàn bộ logic nghiệp vụ, là điểm truy cập duy nhất tới cơ sở dữ liệu quan hệ, đồng thời đóng vai trò cổng trung gian (gateway) an toàn cho các yêu cầu cần tới năng lực xử lý của AI Service. Việc lựa chọn NestJS xuất phát từ kiến trúc module hóa sẵn có của framework (hỗ trợ tách rời Controller, Service và Guard một cách tường minh, phù hợp trực tiếp với mô hình phân lớp trình bày ở Mục 3.3.2) và hệ sinh thái TypeScript giúp giảm thiểu lỗi thời gian chạy khi nhiều thành viên cùng phát triển song song trên các module khác nhau.

\subsection{Mô hình kiến trúc}
Backend được tổ chức theo \textbf{Kiến trúc phân lớp (Layered Architecture)}: một dịch vụ duy nhất, được module hóa rõ ràng theo từng miền nghiệp vụ, chứ không tách thành nhiều dịch vụ vận hành độc lập theo đúng nghĩa Microservices, cũng không áp dụng các mô hình như CQRS (tách biệt luồng xử lý lệnh ghi và truy vấn đọc) hay kiến trúc hướng sự kiện (Event-driven Architecture) với cơ chế phát/nhận sự kiện nghiệp vụ. Lựa chọn này xuất phát từ quy mô của nhóm phát triển (6 thành viên) và thời gian thực hiện đồ án có giới hạn: việc module hóa theo miền nghiệp vụ trong một dịch vụ duy nhất đã đủ để tách biệt rõ trách nhiệm giữa các phần chức năng, trong khi tránh được chi phí vận hành và điều phối phát sinh khi phải triển khai, giám sát và đồng bộ dữ liệu giữa nhiều dịch vụ độc lập như kiến trúc Microservices thực thụ đòi hỏi.

\subsection{Các tầng nội bộ}
Một yêu cầu đi qua Backend lần lượt qua các tầng sau:
\begin{itemize}
    \item \textbf{Tầng tiếp nhận và xác thực yêu cầu:} tiếp nhận yêu cầu từ client, xác thực danh tính người dùng qua JSON Web Token (JWT) và kiểm tra quyền truy cập theo vai trò trước khi cho phép yêu cầu đi tiếp.
    \item \textbf{Tầng chuẩn hóa và giám sát:} chuẩn hóa cấu trúc phản hồi trả về và ghi log của mỗi yêu cầu, phục vụ việc theo dõi và gỡ lỗi hệ thống.
    \item \textbf{Tầng xử lý nghiệp vụ:} nơi hiện thực toàn bộ logic nghiệp vụ cốt lõi của hệ thống, độc lập với cách yêu cầu được tiếp nhận hay dữ liệu được lưu trữ.
    \item \textbf{Tầng truy xuất dữ liệu:} chịu trách nhiệm đọc/ghi dữ liệu trên PostgreSQL, cô lập tầng xử lý nghiệp vụ khỏi chi tiết truy vấn cơ sở dữ liệu.
    \item \textbf{Tầng đẩy tác vụ nền:} đối với các tác vụ không cần phản hồi tức thời (gửi thông báo hàng loạt, lên lịch giao dịch định kỳ), Backend đẩy tác vụ vào hàng đợi Redis/BullMQ thay vì xử lý đồng bộ trong cùng một yêu cầu, giúp không làm chậm tiến trình xử lý API chính.
\end{itemize}

Hình~\ref{fig:backend-layered-architecture} minh họa đường đi của một yêu cầu qua các lớp nội bộ của Backend. Tùy từng miền nghiệp vụ, service có thể truy cập dữ liệu thông qua TypeORM Repository hoặc DataSource; các nhánh xử lý AI và tác vụ nền được tách khỏi luồng truy xuất dữ liệu thông thường.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/backend/backend-layered-architecture.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/backend/backend-layered-architecture.png}%
    }{%
        \rectbox{Sơ đồ kiến trúc phân lớp của NestJS Backend}
    }
    \caption{Kiến trúc phân lớp và các nhánh tích hợp của Backend}
    \label{fig:backend-layered-architecture}
\end{figure}

\subsection{Tổ chức theo miền nghiệp vụ}
Trong phạm vi đề tài này, Backend được tổ chức thành các miền nghiệp vụ chính: Xác thực và phân quyền, Tài chính 6 lọ, Học bổng, Học thuật/Bảng điểm và Thông báo. Cần lưu ý rằng Backend là nền tảng dùng chung cho toàn bộ hệ sinh thái Student360, phục vụ cả các phân hệ khác ngoài phạm vi đồ án này; nội dung trình bày trong báo cáo chỉ tập trung vào các miền nghiệp vụ liên quan trực tiếp tới tài chính, học bổng và học thuật.

\section{Kiến trúc hệ thống AI}

\subsection{Vai trò và vị trí trong kiến trúc tổng thể}
AI Service, hiện thực bằng FastAPI \cite{fastapi-docs} kết hợp thư viện LangChain \cite{langchain-docs} để xây dựng tác nhân AI và kết nối tới mô hình ngôn ngữ lớn Gemini \cite{gemini-api} của Google, cung cấp năng lực xử lý ngôn ngữ tự nhiên cho hệ thống: tư vấn tài chính qua hội thoại, phân loại giao dịch từ mô tả viết tay của người dùng, và gợi ý học bổng phù hợp với hồ sơ sinh viên. Dịch vụ này được tách thành một thành phần độc lập trong kiến trúc tổng thể nhằm cô lập độ trễ và chi phí gọi mô hình ngôn ngữ lớn khỏi luồng xử lý nghiệp vụ lõi của Backend. Client không gọi trực tiếp AI Service; với luồng trò chuyện trực tuyến, Backend tạo JWT có thời hạn ngắn, ký bằng bí mật \texttt{AI\_SERVICE\_SECRET} và gửi token này trong header \texttt{Authorization} khi chuyển tiếp yêu cầu. Việc lựa chọn Python/FastAPI thay vì cùng nền tảng NestJS với Backend xuất phát từ hệ sinh thái thư viện AI/LLM phong phú của Python, trong khi LangChain cung cấp sẵn các thành phần trừu tượng hóa cho tác nhân tool-calling và khả năng tích hợp đa nhà cung cấp mô hình ngôn ngữ, phù hợp trực tiếp với tầng trừu tượng hóa LLM mô tả ở Mục 3.4.3.

\subsection{Luồng xử lý tổng quát}
Khi sinh viên đặt một câu hỏi hoặc nhập một mô tả giao dịch trên Mobile App, yêu cầu được Backend xác thực rồi chuyển tiếp sang AI Service. Tại đây, hệ thống trước tiên phân loại ý định của câu hỏi để xác định ngữ cảnh và phạm vi xử lý phù hợp, sau đó một tác nhân AI (Agent) tiến hành suy luận theo mô hình lặp truy vấn--hành động (ReAct: reasoning + acting) \cite{yao2022-react}, gọi tới các công cụ nghiệp vụ tương ứng khi cần dữ liệu thực tế (thông qua việc gọi ngược lại Backend). Kết quả sau khi được tổng hợp từ mô hình ngôn ngữ lớn được trả về dưới dạng luồng token liên tục cho Backend, và Backend tiếp tục đẩy luồng này về ứng dụng di động để hiển thị theo thời gian thực, tương tự trải nghiệm của các trợ lý ảo thương mại.

\subsection{Các thành phần chính}
\begin{itemize}
    \item \textbf{Tầng tiếp nhận yêu cầu:} nhận yêu cầu từ Backend, xác thực token nội bộ trước khi chuyển tiếp cho tầng xử lý.
    \item \textbf{Tầng phân loại ý định:} xác định loại câu hỏi (truy vấn tài chính, phân loại giao dịch, tư vấn học bổng...) để giới hạn đúng phạm vi ngữ cảnh và tập công cụ được phép sử dụng cho từng loại câu hỏi, thay vì luôn cho phép toàn bộ công cụ có sẵn -- giúp giảm chi phí gọi mô hình và tăng độ chính xác của kết quả.
    \item \textbf{Tầng suy luận và gọi công cụ:} một tác nhân AI duy nhất theo mô hình tool-calling (ReAct), được "thu hẹp phạm vi" linh hoạt theo kết quả phân loại ý định ở bước trước, thay vì cần nhiều tác nhân chuyên biệt tách rời phối hợp với nhau.
    \item \textbf{Tầng công cụ nghiệp vụ:} tập hợp các hàm truy vấn và tính toán cụ thể mà tác nhân AI có thể gọi khi cần dữ liệu thực tế, ví dụ truy vấn số dư hũ tài chính, lịch sử giao dịch, hoặc tính điểm phù hợp giữa hồ sơ sinh viên và một chương trình học bổng.
    \item \textbf{Tầng trừu tượng hóa nhà cung cấp mô hình ngôn ngữ lớn:} cho phép hệ thống chuyển đổi hoặc dự phòng linh hoạt giữa nhiều nhà cung cấp mô hình ngôn ngữ khi gặp lỗi hạn mức hoặc gián đoạn kết nối, mà không ảnh hưởng tới logic nghiệp vụ ở các tầng phía trên.
    \item \textbf{Tầng xử lý nền:} một tiến trình hoàn toàn tách biệt khỏi tiến trình xử lý yêu cầu chính, đảm nhận các tác vụ định kỳ không cần phản hồi tức thời, như tổng hợp nhận định tài chính theo chu kỳ hoặc dọn dẹp các phiên hội thoại không còn hoạt động.
\end{itemize}

Hình~\ref{fig:ai-flow} minh họa luồng xử lý và truyền phản hồi theo thời gian thực nói trên.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/ai/ai-sse-stream.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/ai/ai-sse-stream.png}%
    }{%
        \rectbox{Luồng AI Chat streaming qua Backend và AI Service}
    }
    \caption{Luồng xử lý và truyền phản hồi thời gian thực của AI Service}
    \label{fig:ai-flow}
\end{figure}

\section{Kiến trúc cơ sở dữ liệu}
Hệ thống sử dụng cơ sở dữ liệu quan hệ \textbf{PostgreSQL 15} \cite{postgresql-docs} làm nguồn dữ liệu tin cậy duy nhất (single source of truth) cho toàn bộ dữ liệu nghiệp vụ, được chuẩn hóa theo mô hình quan hệ với các ràng buộc khóa ngoại chặt chẽ để đảm bảo tính toàn vẹn dữ liệu. Bên cạnh đó, \textbf{Redis 7} \cite{redis-docs} đóng vai trò message broker phục vụ hàng đợi xử lý bất đồng bộ, không lưu trữ dữ liệu nghiệp vụ lâu dài.

Dữ liệu của hệ thống được tổ chức thành ba nhóm chính, thể hiện trong sơ đồ ERD ở Hình~\ref{fig:erd-finance}, Hình~\ref{fig:erd-academic} và Hình~\ref{fig:erd-scholarship}:
\begin{itemize}
    \item \textbf{Nhóm Tài chính:} quản lý các hũ tài chính, lịch sử giao dịch thu chi, lịch trình chuyển tiền tự động và cấu hình ngưỡng cảnh báo chi tiêu theo từng hũ.
    \item \textbf{Nhóm Học thuật:} quản lý danh mục trường đại học, chương trình đào tạo, môn học, hồ sơ học thuật và bảng điểm cá nhân của sinh viên.
    \item \textbf{Nhóm Học bổng:} quản lý danh mục và thông tin chương trình học bổng, điều kiện xét tuyển, tài liệu yêu cầu, hồ sơ ứng tuyển của sinh viên, lịch sử chuyển trạng thái và quá trình đánh giá hồ sơ.
\end{itemize}

Về quan hệ giữa các nhóm, nhóm Học bổng tham chiếu tới nhóm Học thuật để đối chiếu điều kiện xét duyệt (ví dụ điểm GPA tối thiểu, ngành học phù hợp) với hồ sơ học thuật thực tế của sinh viên. Nhóm Tài chính được thiết kế độc lập hoàn toàn với hai nhóm còn lại, đúng theo ranh giới nghiệp vụ giữa quản lý dòng tiền cá nhân và quản lý học tập/học bổng.

Trong nhóm Học thuật, mục tiêu tín chỉ không được tách thành bảng riêng. Hai trường \texttt{target\_credits} và \texttt{target\_note} nằm trong \texttt{student\_academic\_profiles}; bảng \texttt{student\_grades} liên kết sinh viên với môn học qua \texttt{student\_id}, \texttt{subject\_id} và lưu điểm tại trường \texttt{grade}. Trong nhóm Học bổng, \texttt{student\_scholarship\_history} ghi nhận \texttt{from\_status} và \texttt{to\_status}, còn \texttt{scholarship\_reviews} lưu kết quả đánh giá của người xét duyệt.

Về mặt thiết kế tài chính, số dư hiện tại được lưu tại trường \texttt{current\_balance} của bảng \texttt{money\_jars} để phục vụ truy vấn và hiển thị nhanh. Sau các thao tác tạo, cập nhật hoặc xóa giao dịch, Backend tính lại tổng thu và tổng chi từ lịch sử giao dịch rồi cập nhật số dư và các giá trị thu, chi lũy kế. Cách làm này kết hợp hiệu năng đọc với khả năng khôi phục tính nhất quán từ dữ liệu giao dịch gốc. Cơ chế xử lý các trường hợp đặc biệt, như xóa một hũ đang có số dư, được trình bày chi tiết cùng đặc tả chức năng liên quan ở Chương 4.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/finance/finance-erd.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/finance/finance-erd.png}%
    }{%
        \rectbox{Sơ đồ ERD cho phân hệ Tài chính 6 lọ}
    }
    \caption{Sơ đồ thực thể liên kết (ERD) cho mô-đun Tài chính 6 lọ}
    \label{fig:erd-finance}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/academic/academic-erd.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/academic/academic-erd.png}%
    }{%
        \rectbox{Sơ đồ ERD cho phân hệ học thuật và bảng điểm}
    }
    \caption{Sơ đồ ERD cho phân hệ học thuật và bảng điểm}
    \label{fig:erd-academic}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/scholarship/scholarship-erd.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/scholarship/scholarship-erd.png}%
    }{%
        \rectbox{Sơ đồ ERD cho phân hệ học bổng}
    }
    \caption{Sơ đồ ERD cho phân hệ học bổng}
    \label{fig:erd-scholarship}
\end{figure}

\section{Luồng tương tác giữa các thành phần}
Phần này mô tả bốn luồng tương tác tiêu biểu giữa các thành phần kiến trúc, ở góc nhìn thành phần nào tham gia và giao tiếp qua kênh nào; đặc tả chi tiết từng bước nghiệp vụ và endpoint cụ thể của các luồng này được trình bày tại Chương 4.

\subsection{Luồng ghi nhận và phân loại giao dịch bằng AI}
Sinh viên nhập một mô tả giao dịch bằng ngôn ngữ tự nhiên trên Mobile App. Yêu cầu được gửi tới Backend qua HTTP REST; Backend chuyển tiếp sang AI Service để phân loại; AI Service trả về hũ tài chính và danh mục chi tiêu được đề xuất; Backend chuyển kết quả này về Mobile App để sinh viên xác nhận trước khi lưu giao dịch vào PostgreSQL.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/finance/ai-transaction-classification-flow.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/finance/ai-transaction-classification-flow.png}%
    }{%
        \rectbox{Luồng ghi nhận và phân loại giao dịch bằng AI}
    }
    \caption{Luồng phân loại và xác nhận giao dịch tài chính bằng AI}
    \label{fig:ai-transaction-classification-flow}
\end{figure}

\subsection{Luồng trò chuyện AI thời gian thực}
Sinh viên đặt câu hỏi trong màn hình trò chuyện AI. Mobile App mở một kết nối SSE tới Backend; Backend xác thực rồi chuyển tiếp yêu cầu sang AI Service; AI Service suy luận và truy vấn dữ liệu thật qua Backend khi cần, sau đó truyền câu trả lời về dưới dạng luồng token liên tục; Backend chuyển tiếp luồng này về Mobile App để hiển thị theo thời gian thực (Hình~\ref{fig:ai-flow}).

\subsection{Luồng sinh viên nộp hồ sơ học bổng}
Sinh viên mở chi tiết học bổng trên Mobile App, nhập thông tin ứng tuyển và upload tài liệu minh chứng. Mobile App gửi hồ sơ qua \texttt{POST /scholarships/register}; Backend kiểm tra hạn nộp, hồ sơ học thuật, GPA và ràng buộc không nộp trùng, sau đó lưu hồ sơ, tài liệu và lịch sử trạng thái trong cùng một transaction. Hình~\ref{fig:scholarship-student-application-flow} thể hiện luồng tương tác giữa sinh viên, Mobile App, dịch vụ upload, Backend và PostgreSQL.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/scholarship/scholarship-student-flow.png}{%
        \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{Images/chapter3/scholarship/scholarship-student-flow.png}%
    }{%
        \rectbox{Luồng sinh viên nộp hồ sơ học bổng}
    }
    \caption{Luồng sinh viên chuẩn bị và nộp hồ sơ học bổng}
    \label{fig:scholarship-student-application-flow}
\end{figure}

\subsection{Luồng xét duyệt học bổng và thông báo kết quả}
Nhà tài trợ cập nhật trạng thái một hồ sơ ứng tuyển trên Web Admin Portal qua \texttt{PUT /student-scholarships/\{id\}/status}. Backend cập nhật trạng thái và lịch sử hồ sơ trong PostgreSQL; với các trạng thái kết quả \texttt{approved}, \texttt{rejected} hoặc \texttt{awarded}, hệ thống đồng thời ghi nhận ngày ra quyết định. Sau mỗi lần cập nhật trạng thái thành công, Backend tạo notification job, lọc người nhận theo tùy chọn thông báo và đẩy tác vụ vào BullMQ/Redis. Worker lấy tác vụ, gọi Firebase Cloud Messaging và cập nhật trạng thái job thành \texttt{SENT} hoặc \texttt{FAILED}.

\begin{figure}[H]
    \centering
    \IfFileExists{Images/chapter3/scholarship/scholarship-review-notification-flow.png}{%
        \includegraphics[width=\textwidth,height=0.82\textheight,keepaspectratio]{Images/chapter3/scholarship/scholarship-review-notification-flow.png}%
    }{%
        \rectbox{Luồng xét duyệt học bổng và gửi thông báo qua BullMQ/FCM}
    }
    \caption{Luồng xét duyệt hồ sơ học bổng và thông báo kết quả}
    \label{fig:scholarship-review-notification-flow}
\end{figure}

\section{Các vấn đề kỹ thuật khó và giải pháp}
\label{sec:cac-van-de-ky-thuat}

Student360 là đồ án được hợp nhất từ ba nhóm phát triển tương ứng với ba phân hệ chính: E-learning, Career và Finance. Vì vậy, bên cạnh các vấn đề thuần túy về lập trình, nhóm còn phải giải quyết khó khăn trong giao tiếp, phân chia phạm vi và tích hợp các thành phần được phát triển song song. Các vấn đề kỹ thuật tiếp theo liên quan tới truyền dữ liệu thời gian thực, tính nhất quán của dữ liệu tài chính và học bổng, xử lý tác vụ bất đồng bộ, import dữ liệu học thuật và đồng bộ trạng thái trên giao diện. Các giải pháp dưới đây được lựa chọn dựa trên kiến trúc hiện tại và giới hạn thời gian của đồ án; đối với những vấn đề chưa được xử lý triệt để, nhóm đồng thời ghi nhận giới hạn còn lại để làm cơ sở cải tiến.

\begin{enumerate}
    \item \textbf{Phối hợp và tích hợp kết quả phát triển của ba nhóm E-learning, Career và Finance.}

    \textit{Vấn đề:} Ba nhóm phát triển song song các phân hệ khác nhau nhưng cùng sử dụng tài khoản sinh viên, hồ sơ cá nhân, dữ liệu học thuật, Backend và cơ sở dữ liệu của Student360. Sự khác biệt về cách đặt tên trường dữ liệu, trạng thái nghiệp vụ, cấu trúc phản hồi API và thời điểm cập nhật chức năng có thể làm cho một phân hệ hoạt động độc lập nhưng phát sinh lỗi khi ghép vào hệ thống chung. Việc trao đổi qua nhiều kênh cũng dễ dẫn tới bỏ sót thay đổi, triển khai sai phiên bản hoặc không xác định rõ nhóm chịu trách nhiệm khi lỗi đi qua ranh giới giữa các phân hệ.

    \textit{Giải pháp:} Nhóm phân chia ranh giới trách nhiệm theo từng miền nghiệp vụ, đồng thời xác định rõ các thành phần dùng chung và người phụ trách đầu mối tích hợp. Hợp đồng API được thống nhất trước khi hiện thực, bao gồm endpoint, cấu trúc request/response, kiểu dữ liệu, mã lỗi và tập trạng thái nghiệp vụ; mọi thay đổi ảnh hưởng tới nhóm khác phải được thông báo và xem xét qua Pull Request. Công việc được đồng bộ theo sprint một tuần, các vướng mắc liên phân hệ được đưa vào backlog và trao đổi trên kênh chung thay vì xử lý riêng lẻ. Trước khi hợp nhất một chức năng, nhóm kiểm thử luồng xuyên suốt giữa Mobile App hoặc Web Admin, Backend và cơ sở dữ liệu trên cùng môi trường tích hợp. Cách tổ chức đa-repository giúp các nhóm phát triển độc lập, trong khi quy ước API và kiểm thử liên phân hệ giúp hạn chế sai lệch khi ghép ba phần thành một hệ thống thống nhất.

    \item \textbf{Truyền phản hồi AI theo thời gian thực trên React Native.}

    \textit{Vấn đề:} Luồng trò chuyện AI phải đi qua hai chặng Mobile App--Backend và Backend--AI Service. Trên React Native native, cơ chế \texttt{fetch} không cung cấp \texttt{ReadableStream} ổn định như trên trình duyệt; nếu chờ toàn bộ phản hồi hoàn tất mới hiển thị thì thời gian chờ cảm nhận của người dùng tăng cao. Ngoài ra, kết nối cần được đóng đúng cách khi người dùng rời màn hình để tránh tiếp tục tiêu thụ tài nguyên.

    \textit{Giải pháp:} Mobile App sử dụng cơ chế XHR với kiểu phản hồi dạng văn bản, xử lý dữ liệu tăng dần qua \texttt{onprogress} và bổ sung \texttt{readyState=3} làm cơ chế dự phòng trên các phiên bản React Native mà \texttt{onprogress} hoạt động không ổn định. Backend chuẩn hóa luồng thành các sự kiện SSE gồm \texttt{status}, \texttt{token}, \texttt{done} và \texttt{error}; đồng thời cấu hình không lưu cache, duy trì kết nối và tắt buffering tại proxy. Khi client đóng kết nối, Backend hủy subscription tới luồng xử lý hiện tại. Nhờ đó, nội dung được hiển thị từng phần và người dùng có thể chủ động hủy yêu cầu đang chạy.

    \item \textbf{Bảo đảm tính nhất quán khi thao tác với nhiều lọ tài chính.}

    \textit{Vấn đề:} Một lần chuyển tiền giữa hai lọ phải tạo đồng thời một giao dịch chi ở lọ nguồn và một giao dịch thu ở lọ đích. Nếu chỉ một bản ghi được tạo thành công, số dư và báo cáo tài chính sẽ sai lệch. Bên cạnh đó, các giao dịch chuyển lọ không được tính như thu nhập hoặc chi tiêu thực tế khi tổng hợp báo cáo.

    \textit{Giải pháp:} Backend sử dụng \texttt{QueryRunner} của TypeORM để tạo hai bản ghi giao dịch trong cùng một database transaction; nếu một thao tác thất bại, toàn bộ transaction được rollback. Hai giao dịch được liên kết qua \texttt{counterpart\_jar\_id}, và truy vấn tổng hợp loại các giao dịch chuyển lọ để tránh tính trùng thu/chi. Số dư lọ được tính lại từ lịch sử giao dịch sau khi ghi thành công thay vì chỉ tin vào giá trị tạm trên giao diện. Đối với phân bổ một khoản thu vào nhiều lọ, hệ thống kiểm tra quyền sở hữu từng lọ, tổng số tiền phân bổ và xử lý phần dư do làm tròn. Tuy nhiên, các bản ghi của luồng phân bổ thu nhập hiện vẫn được tạo song song mà chưa nằm trong một transaction chung; đây là rủi ro còn lại cần khắc phục.

    \item \textbf{Lưu hồ sơ học bổng gồm nhiều loại dữ liệu nhưng không phát sinh hồ sơ trùng.}

    \textit{Vấn đề:} Một lần lưu hoặc nộp hồ sơ có thể đồng thời cập nhật CCCD, GPA, hồ sơ ứng tuyển, danh sách tài liệu và lịch sử trạng thái. Nếu lỗi xảy ra giữa chừng, dữ liệu có thể ở trạng thái không hoàn chỉnh. Hệ thống cũng phải hỗ trợ lưu nháp, nộp lại hồ sơ cần bổ sung và ngăn sinh viên tạo nhiều hồ sơ cho cùng một học bổng.

    \textit{Giải pháp:} Bảng \texttt{student\_scholarships} đặt ràng buộc duy nhất trên cặp \texttt{user\_id} và \texttt{scholarship\_id}. Backend thực hiện cập nhật hồ sơ cá nhân, tạo hoặc cập nhật đơn, thay thế tài liệu và ghi \texttt{student\_scholarship\_history} trong cùng một transaction của TypeORM. Trước khi nộp chính thức, hệ thống kiểm tra hạn nộp, mức độ hoàn chỉnh của hồ sơ và GPA tối thiểu; các trạng thái được phép chỉnh sửa được xác định rõ để phục vụ lưu nháp hoặc yêu cầu bổ sung. Giới hạn hiện tại là kiểm tra ngành học và trường mục tiêu vẫn mới ở mức TODO trong Backend, còn kiểm tra đủ tài liệu bắt buộc chủ yếu được thực hiện trên Mobile App.

    \item \textbf{Gửi thông báo mà không làm tăng thời gian phản hồi của API nghiệp vụ.}

    \textit{Vấn đề:} Việc gọi Firebase Cloud Messaging phụ thuộc mạng và có thể mất nhiều thời gian hoặc thất bại tạm thời. Nếu gửi thông báo đồng bộ ngay trong API cập nhật trạng thái học bổng hoặc cảnh báo tài chính, thời gian phản hồi của nghiệp vụ chính sẽ bị phụ thuộc vào dịch vụ bên thứ ba.

    \textit{Giải pháp:} Backend tách quá trình gửi thành mô hình producer--consumer sử dụng BullMQ và Redis. Producer kiểm tra tài khoản nhận, tùy chọn thông báo, tạo bản ghi job và đẩy payload vào queue \texttt{notification}; worker nền lấy job, gọi dịch vụ gửi thông báo và cập nhật trạng thái \texttt{PROCESSING}, \texttt{SENT} hoặc \texttt{FAILED}. Queue hỗ trợ thời điểm gửi và giữ lại job lỗi để phục vụ theo dõi. Nhờ đó, API nghiệp vụ chỉ cần hoàn tất cập nhật dữ liệu và tạo job, không phải chờ Firebase phản hồi.

    \item \textbf{Import bảng điểm từ tệp không đồng nhất.}

    \textit{Vấn đề:} File TXT/CSV của sinh viên có thể dùng các loại mã môn khác nhau, thứ tự cột khác nhau hoặc chứa dữ liệu sai định dạng. Nếu gửi trực tiếp toàn bộ nội dung lên Backend, sinh viên khó phát hiện bản ghi ánh xạ sai trước khi lưu.

    \textit{Giải pháp:} Mobile App đọc và phân tách file tại client, yêu cầu có cột điểm và ít nhất một trong các khóa \texttt{subject\_id}, \texttt{subject\_code} hoặc \texttt{course\_code}. Dữ liệu được ánh xạ với danh mục môn của trường, hiển thị trong bước xem trước và cho phép sửa hoặc xóa từng dòng trước khi tạo payload có cấu trúc gửi tới Backend. Sau khi lưu, các truy vấn bảng điểm, dashboard và mục tiêu tín chỉ được làm mới. Cách xử lý này giảm dữ liệu lỗi đi vào hệ thống, nhưng Backend vẫn cần được tăng cường kiểm tra thang điểm và tính đồng nhất khi dữ liệu có cả thang 4 và thang 10.

    \item \textbf{Đồng bộ nhiều màn hình sử dụng chung dữ liệu sau thao tác ghi.}

    \textit{Vấn đề:} Một giao dịch tài chính mới đồng thời ảnh hưởng tới lịch sử giao dịch, số dư lọ, biểu đồ tổng hợp, báo cáo và cảnh báo. Tương tự, một điểm số mới ảnh hưởng tới danh sách điểm, GPA và tiến độ tín chỉ. Nếu chỉ cập nhật màn hình hiện tại, các màn hình khác có thể tiếp tục hiển thị cache cũ.

    \textit{Giải pháp:} Mobile App quản lý dữ liệu server bằng React Query và định nghĩa query key theo từng miền. Sau mutation, ứng dụng chủ động invalidate các nhóm dữ liệu liên quan như giao dịch, lọ tài chính, tổng hợp, báo cáo, thông báo hoặc bảng điểm/dashboard; dữ liệu được lấy lại từ Backend thay vì tự cộng trừ tại nhiều màn hình. Cơ chế này ưu tiên tính nhất quán và giảm việc nhân bản logic tính toán ở tầng giao diện.
\end{enumerate}

\section{Đánh giá kiến trúc hệ thống}

\subsection{Ưu điểm}
Kiến trúc phân tán nhiều tầng, đa-repository giúp tách trách nhiệm rõ ràng theo cả chiều tầng (trình bày/ứng dụng/dữ liệu) lẫn chiều dịch vụ (Mobile, Web Admin, Backend, AI Service). Mỗi cặp thành viên trong nhóm có thể phát triển độc lập trên repository riêng của mình, hạn chế xung đột mã nguồn khi làm việc song song (liên kết với cách phân công tại Chương 5). Việc tách AI Service thành một dịch vụ riêng giúp cô lập hoàn toàn rủi ro về độ trễ, lỗi hoặc chi phí gọi mô hình ngôn ngữ lớn khỏi các nghiệp vụ lõi (tài chính, học bổng), đảm bảo các chức năng cốt lõi vẫn hoạt động ổn định ngay cả khi thành phần AI gặp sự cố. Việc xử lý bất đồng bộ qua hàng đợi giúp hệ thống chịu tải tốt khi cần gửi thông báo tới số lượng lớn người dùng cùng lúc mà không làm chậm các yêu cầu API chính.

\subsection{Khả năng mở rộng và bảo trì}
Kiến trúc phân lớp và module hóa theo miền nghiệp vụ cho phép bổ sung dịch vụ hoặc miền nghiệp vụ mới mà không phá vỡ cấu trúc hiện có. Tầng trừu tượng hóa nhà cung cấp mô hình ngôn ngữ lớn tại AI Service cho phép đổi hoặc bổ sung nhà cung cấp mới mà không cần sửa đổi logic nghiệp vụ ở các tầng phía trên. Việc tổ chức đa-repository, mỗi dịch vụ một repository riêng, giúp giảm xung đột mã nguồn giữa các nhóm nhỏ và cho phép mỗi dịch vụ được triển khai, phiên bản hóa độc lập với các dịch vụ còn lại.

\subsection{Hướng mở rộng kiến trúc trong tương lai}
Mã nguồn của AI Service hiện đã chuẩn bị sẵn một khung điều phối đa-agent (multi-agent orchestration), cho phép định tuyến yêu cầu tới nhiều tác nhân AI chuyên biệt theo từng miền nghiệp vụ khác nhau; tuy nhiên khung này chưa được kích hoạt trong luồng xử lý thực tế ở phiên bản hiện tại, mà chỉ mới sử dụng một tác nhân duy nhất với cơ chế phân loại ý định như đã mô tả ở Mục 3.4. Đây là nền tảng sẵn có để hệ thống có thể mở rộng thành kiến trúc đa-agent thực thụ khi bổ sung thêm các nghiệp vụ AI phức tạp hơn trong tương lai.

\subsection{Giới hạn hiện tại}
Nhìn từ góc độ kiến trúc, hệ thống còn một số giới hạn: một số thao tác tài chính tổng hợp (như phân bổ thu nhập vào nhiều hũ cùng lúc) chưa được bọc trong một transaction cơ sở dữ liệu tường minh, tiềm ẩn rủi ro không nhất quán nếu xảy ra lỗi giữa chừng (chi tiết tại Chương 4); hệ thống chưa có bộ benchmark định lượng chính thức, có thể tái lập cho các thành phần AI (chi tiết tại Chương 6); và hệ thống phụ thuộc vào các dịch vụ bên thứ ba nằm ngoài tầm kiểm soát trực tiếp (Firebase Cloud Messaging, nhà cung cấp mô hình ngôn ngữ lớn).
